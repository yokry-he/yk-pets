# @yk-pets/pet-github-provider

Strict, repository-scoped GitHub collaboration adapter. The host exposes fixed commands only; arbitrary REST, GraphQL, URLs, tokens, shell commands, and script execution are not part of the contract.
