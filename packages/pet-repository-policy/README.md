# @yk-pets/pet-repository-policy

Fail-closed pre-commit policy for isolated YK Pets worktrees. It verifies the exact approved path set, branch naming, protected branches and paths, staged state, byte/file budgets, base revision, required validation evidence, secret-scan findings, and commit subject before a commit can be created.
