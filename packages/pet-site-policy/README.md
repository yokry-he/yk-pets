# @yk-pets/pet-site-policy

Framework-independent per-site policy storage and resolution for YK Pets. Supports exact origins, wildcard hosts, priorities, temporary session overrides, import/export, and a structural `chrome.storage` adapter.
