# @yk-pets/pet-remote-release

Coordinates remote pull-request lifecycle actions. Every mutating action requires a short-lived, one-time approval bound to repository, PR number, exact head SHA, lifecycle snapshot, action, and resource digest.
