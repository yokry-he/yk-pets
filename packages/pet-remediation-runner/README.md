# @yk-pets/pet-remediation-runner

Orchestrates patch validation, dry-run preview, one-time scope approval, compare-and-swap file transaction, optional verification, and automatic rollback. Required verification without an adapter, failed verification, thrown verification, timeout, or abort all fail closed and trigger rollback using an independent recovery path.
