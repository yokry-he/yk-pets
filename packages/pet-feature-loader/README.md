# @yk-pets/pet-feature-loader

Loads split extension features only when needed. Concurrent calls are deduplicated, dependencies are ordered, cycles are rejected, timeouts abort work, and a safe fallback may be supplied.
