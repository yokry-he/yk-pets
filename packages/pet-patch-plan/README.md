# @yk-pets/pet-patch-plan

Defines `yk-pets.patch-plan/v1`: deterministic source changes, normalized project-relative paths, SHA-256 preconditions, non-overlapping text edits, plan digests, and touched-path summaries. It never reads or writes a workspace by itself.
