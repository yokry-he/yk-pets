# @yk-pets/pet-change-report

Builds deterministic `yk-pets.change-report/v1` reports in JSON and Markdown from analysis findings, DOM-to-source candidates, source changes, verification results, rollback instructions, and audit events.

Duplicate findings are merged by fingerprint and sensitive tokens are redacted from user-visible output.
