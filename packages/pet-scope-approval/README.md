# @yk-pets/pet-scope-approval

Issues and verifies HMAC-signed `yk-pets.scope-approval/v1` tokens bound to one patch plan digest, subject, project, optional origin/base revision, exact file or directory scopes, operation set, file count, byte budget, expiration, and one-time replay protection.
