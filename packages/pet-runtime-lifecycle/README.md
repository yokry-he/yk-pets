# @yk-pets/pet-runtime-lifecycle

Coordinates renderer activity with page visibility, viewport intersection, freeze/pagehide events, manual pause, and per-site modes. Includes an idle-task scheduler with a timeout fallback.
