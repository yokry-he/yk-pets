# @yk-pets/pet-source-mapper

Maps inspected DOM elements back to Vue source candidates using stable selectors, Vue 2/3 runtime ownership, source-inspector attributes, component registries, stack frames, and caller-supplied Source Map v3 files.

The package never fetches maps or evaluates page code. Source maps and runtime hints are supplied by an explicitly authorized host.
