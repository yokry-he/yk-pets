# @yk-pets/pet-plugin-registry

Validates `yk-pets.plugin/v1` manifests, checks YK Pets/pet-core/API versions, resolves dependencies, detects cycles and capability conflicts, and manages deterministic activation/deactivation.
