/**
 * 文件职责 / File responsibility
 * 扩展端沿用 Playground 的动作部位节点解析，避免两套绑定语义发生漂移。
 * Reuses Playground motion-part node resolution in Extension so binding semantics cannot drift.
 */
export * from '../../playground/app/composables/useStudioMotionPartNodes'
