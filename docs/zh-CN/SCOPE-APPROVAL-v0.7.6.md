# 修改范围审批 v0.7.6

审批令牌采用 HMAC-SHA256 签名，密钥至少 32 字节，并应仅存放在扩展 Background、桌面宿主或 CI Secret 中。

令牌绑定：用户、项目、计划 ID、计划摘要、签发/过期时间、操作类型、精确文件或目录范围、最多文件数、最多写入字节、可选 Origin 和基线版本。令牌消费后写入防重放存储，不能再次使用。

推荐先调用 `FileTransactionExecutor.preview()`，再用实际 `changedBytes` 和触及文件签发审批。审批失败、过期、计划变化或事务失败后都应重新向用户展示预览并重新审批。
