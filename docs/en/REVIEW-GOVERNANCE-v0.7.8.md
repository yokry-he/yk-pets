# Review Governance v0.7.8

Review plans are bound to repository, PR, head SHA, lifecycle digest, thread ID, and the latest comment ID. Only reply and resolve operations are supported. Partial remote mutations are reported explicitly.
