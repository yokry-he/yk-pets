# v0.7.3 Plugin Manifest and Compatibility

`yk-pets.plugin/v1` declares engine ranges, entrypoints, capabilities, permissions, dependencies, and optional integrity. Registration validates host compatibility; activation performs topological ordering, cycle detection, dependency ranges, capability conflicts, and lifecycle handling.
