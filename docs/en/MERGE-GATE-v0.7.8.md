# Merge Gate v0.7.8

The merge gate returns eligible, waiting, or blocked. Pending checks and unknown mergeability wait; stale scope, conflicts, failed or ambiguous required checks, changes requested, unresolved threads, and insufficient approvals block.
