# DOM to Vue source mapping

`SourceLocator` ranks explicit inspector metadata, Vue 2/3 runtime ownership, registered Source Map v3 results, component registries, and direct source-like stack frames. Maps are supplied by the host; this package never fetches them.
