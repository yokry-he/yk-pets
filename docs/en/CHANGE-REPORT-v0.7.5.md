# Structured change reports

`ChangeReportBuilder` emits deterministic `yk-pets.change-report/v1` JSON and Markdown with deduplicated findings, source candidates, file changes, rollback instructions, verification results, timelines, redaction, and a stable fingerprint.
