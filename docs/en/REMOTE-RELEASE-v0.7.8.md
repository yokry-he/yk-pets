# Remote Release Lifecycle v0.7.8

The coordinator performs failed-check retry, approved review plans, merge after an eligible gate, and post-merge branch/worktree cleanup. Every mutation consumes an exact one-time `yk-pets.remote-approval/v1` token.
