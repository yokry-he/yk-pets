# GitHub Provider v0.7.8

`GitHubProvider` accepts only fixed `yk-pets.github-provider/v1` commands for PRs, checks, reviews, controlled retry, thread reply/resolve, merge, close, and exact branch deletion. The trusted host supplies the invoker and credentials; page code never receives arbitrary API or token access.
