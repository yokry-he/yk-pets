# NOVA 控制切换器位置修复（v0.2.9）

## 目标

“功能 / 动作”切换器不再根据提示卡是否显示而在顶部和宠物附近之间跳动。

## 实现

- 桌面端固定在宠物上方 `top: 96px`。
- 菜单关闭按钮固定在同一水平线附近 `top: 98px`。
- 提示卡显示或隐藏时，切换器和菜单关闭按钮保持稳定。
- 窄屏模式使用 `top: 92px`，关闭按钮使用 `top: 94px`。
- 仅提示卡自己的关闭按钮跟随提示卡，不影响菜单控制区。

## 结果

控制入口保持在用户截图中图一的位置附近，并向上微调；不会再出现图二中与宠物、环形按钮距离过远的情况。
