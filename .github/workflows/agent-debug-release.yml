name: Agent Debug Phase E Release

on:
  pull_request:
    types:
      - synchronize

permissions:
  actions: read
  contents: read

jobs:
  debug:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'agent/cloud-fox-studio-v0610' }}"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Download failed phase E logs
        env:
          GH_TOKEN: ${{ github.token }}
          FAILED_JOB_ID: '89660011072'
        shell: bash
        run: |
          set -euo pipefail
          curl --fail-with-body --location --silent --show-error \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/jobs/$FAILED_JOB_ID/logs" \
            --output /tmp/failed-job.log
          {
            echo '=== ERROR MATCHES ==='
            grep -nEi '(^|[^a-z])(error|failed|failure|ERR_|ELIFECYCLE|TS[0-9]{4}|AssertionError|RuntimeError)([^a-z]|$)' /tmp/failed-job.log | tail -n 240 || true
            echo
            echo '=== LAST 460 LINES ==='
            tail -n 460 /tmp/failed-job.log
          } > /tmp/phase-e-error.txt
          cat /tmp/phase-e-error.txt
      - name: Upload diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: phase-e-release-error
          path: /tmp/phase-e-error.txt
          retention-days: 1
